# Vanilla Bubbles

TypeScript + Vite + `@chenglou/pretext`.

This example shows the smallest real integration path:

- wait for fonts
- `prepare()` once
- `layout()` on resize
- keep DOM reads out of the text relayout path
