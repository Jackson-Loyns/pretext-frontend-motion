# React Bubbles

React 19 + TypeScript + Vite + `@chenglou/pretext`.

This example keeps Pretext inside a dedicated hook so the component tree does not mix layout measurement with rendering concerns.
