# Platform Notes

Each file in this directory documents one install target:

- root folder under the home directory
- bundle install path
- entry filename

These notes mirror the machine-readable metadata in `platforms/*.json`.
