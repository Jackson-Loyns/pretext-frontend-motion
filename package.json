{
  "name": "pretext-frontend-motion-repo",
  "private": true,
  "workspaces": [
    "packages/cli"
  ],
  "dependencies": {
    "pretext-skill": "file:packages/cli"
  },
  "scripts": {
    "build": "npm run build --workspace packages/cli",
    "test": "npm run test --workspace packages/cli",
    "cli": "node packages/cli/dist/index.js"
  }
}
